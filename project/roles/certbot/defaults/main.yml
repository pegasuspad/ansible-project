---
# certbot_admin_email: email address used to agree to Let's Encrypt's TOS and subscribe to cert-related notifications
certbot_admin_email: sean@pegasuspad.com

# certbot_domains: list of certs to generate, each of which should have a 'domains' property that is the domain names to include
certbot_certs: []

# certbox_proxy_host: name of the reverse proxy host or group to reprovision when certs change, or null to skip reprovisioning
certbot_proxy_host: null

# certbot_testmode: if true, obtain a test cert from staging server
certbot_testmode: false

# certbot_updated_domains_file: full path to the file containing the list of domains with updated (but not deployed) certs
certbot_updated_domains_file: '/etc/letsencrypt/updated-certs.json'

# certbot_webhook_host: hostname for the put-secret webhook, or null to skip updating the vault
certbot_webhook_host: null
# certbot_webhook_token: token used to authenticate with the 'put-secret' webhook
certbot_webhook_token: null
# certbot_webhook_port: port number on which the put-secret webhook is listening
certbot_webhook_port: 9000

# disable geerlingguy's auto-renewal cron, since certbot does this automatically
certbot_auto_renew: false
# certbot_auto_renew_user: "root"
# certbot_auto_renew_hour: "3"
# certbot_auto_renew_minute: "30"
# certbot_auto_renew_options: "--quiet"