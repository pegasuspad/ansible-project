all:
  children:
    ansible_control_nodes:
      hosts:
        lab-ansible:
      vars:
        ansible_connection: local
    certbot:
      hosts:
        lab-certbot:
    nextcloud_servers:
      hosts:
        lab-nextcloud:
    reverse_proxy:
      hosts:
        lab-proxy:
